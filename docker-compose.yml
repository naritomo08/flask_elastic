services:
  web:
    build: .
    ports:
      - "5001:5000"
    environment:
      ELASTICSEARCH_URL: http://elastic1:9200
      ELASTICSEARCH_INDEX: logs-*
    extra_hosts:
      - "elastic1:192.168.11.20"
